def soma(x,z):
        try:
                soma = float(x) + float (z)
                return soma
        except:                 
                return None


def sub(x,z):

        return float(x) - float (z)

def mult(x,z):

       return float(x) * float(z)


def div(x,z):

        return float (x) / float (z)
    
